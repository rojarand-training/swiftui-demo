source 'https://cdn.cocoapods.org/'

use_frameworks!

install! 'cocoapods', :disable_input_output_paths => true, :share_schemes_for_development_pods => true

target 'SwiftUITipsAndTricks' do
    pod 'PodFlagsLib2', :path => '.', :testspecs => ['Tests'] 
end

target 'SwiftUITipsAndTricksTests' do
    pod 'PodFlagsLib2', :path => '.', :testspecs => ['Tests'] 
end